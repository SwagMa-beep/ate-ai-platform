# Optional. Frontend API origin used when the backend is not served from the
# same host as the Vite dev server.
#
# In normal local development this can stay empty because Vite proxies /api,
# /files, and /health to http://localhost:8000.
VITE_API_ORIGIN=
